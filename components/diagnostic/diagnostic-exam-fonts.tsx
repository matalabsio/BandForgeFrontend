import { GoogleFontsLink } from "@/components/fonts/google-fonts-link";

/** Loads passage fonts for diagnostic exam modules only (not landing). */
export function DiagnosticExamFonts() {
  return <GoogleFontsLink />;
}
