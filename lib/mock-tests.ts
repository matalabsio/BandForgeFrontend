/** @deprecated Use `@/lib/listening-test` — kept for imports during migration. */
export {
  LISTENING_TEST_ID as GREENFIELD_LISTENING_MOCK_ID,
  listeningTestPath as mockListeningPath,
} from "@/lib/listening-test";
