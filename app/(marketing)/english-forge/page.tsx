import { EnglishForgeLanding } from "@english/components/english-forge-landing";

export const metadata = {
  title: "English Forge — Spoken English for school",
  description:
    "Kind Spoken English coaching for Class 1–10. Listen, speak, get clear feedback — no exam bands.",
};

/** Hero landing — product code in repo-root `English/`. */
export default function EnglishForgePage() {
  return <EnglishForgeLanding />;
}
